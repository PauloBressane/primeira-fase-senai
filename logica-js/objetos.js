const usuario = {
    login:"username",
    email:"email@mail.com",
    saldo:50000.00,
    islogin:true,
    senha:"123456789"
}

console.log(usuario.email)

console.log(`o email do usuario é ${usuario.email} o saldo do usuario é ${usuario.saldo}`)
